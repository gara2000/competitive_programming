
	// Read a string until \n
	//char t;
	//scanf("%s", t);
	//scanf("%[^\n]", t);
	// Note: scanf returns the numbers of items read
	// can be used as while(scanf(...)>0){}
	// printf("%d", digit)
	// int a, b, c;
	// scanf("%d %d %d", &a, &b, &c);

	// char str[1001];
	// scanf("%[^\n]\n", str);
	// string name;
	// getline(cin, name);
	// cin.ignore();